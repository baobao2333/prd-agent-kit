interface:
  display_name: "PRD HTML Handoff Artifact"
  short_description: "Create readable PRD HTML handoff artifacts"
  default_prompt: "Create a standalone HTML handoff artifact from this completed PRD workspace while preserving Markdown as the source of truth."
